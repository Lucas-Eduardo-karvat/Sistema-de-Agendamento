spring.application.name=agendamento-api

# ---- Banco de dados (PostgreSQL / Neon) ----
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-late-cloud-ace43zrx-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USER:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_AwbMjkTD7Fe8}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

spring.docker.compose.enabled=false

server.port=8080